spring.application.name=java-monitoring-app
# App port
server.port=8080

# Actuator settings
management.endpoints.web.exposure.include=*
management.endpoint.prometheus.enabled=true
management.metrics.export.prometheus.enabled=true
